doctype html
html
  //- var baseUrl = taskTarget === 'tmp' ? '/' : 'https://CatalinaTeguiani.github.io/w3school-replica/'
  //- - var baseUrl = 'http://localhost:3000'
  - var baseUrl = 'https://CatalinaTeguiani.github.io/w3schools-replica'
  block cool-config
  head
    title=myTitle
    meta(name="description", content=myDescription)
    link(rel="stylesheet", href=`${baseUrl}style.css` )
    //- if config.entry.css.embed
    //-   link(rel="stylesheet", href=`${embedPath}`, inline)
  body
    include ../header/header

    article
      block awesome-content
  
    include ../nav/nav
    include ../footer/footer
